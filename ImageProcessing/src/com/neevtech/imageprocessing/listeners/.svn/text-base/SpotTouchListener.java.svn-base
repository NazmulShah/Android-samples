<<<<<<< HEAD
/**
 * 
 */
package com.neevtech.imageprocessing.listeners;

import android.content.Context;
import android.graphics.Bitmap;
import android.graphics.Color;
import android.graphics.drawable.BitmapDrawable;
import android.graphics.drawable.Drawable;
import android.view.MotionEvent;
import android.view.View;
import android.view.View.OnTouchListener;
import android.widget.ImageView;
import android.widget.Toast;

/**
 * @author Ashish R Agre
 * 
 */
public class SpotTouchListener implements OnTouchListener {

	private static int COLOR_MAX = 0XFF;
	private Context context;
	private ImageView imageView;

	public SpotTouchListener(ImageView imageView, Context context) {
		this.imageView = imageView;
		this.context = context;
	}

	@Override
	public boolean onTouch(View v, MotionEvent event) {
		/*
		 * int xTouch = (int) event.getX(); int yTouch = (int) event.getY();
		 * Drawable tempDrawable = imageView.getDrawable(); Bitmap bitmap =
		 * ((BitmapDrawable) tempDrawable).getBitmap(); int width =
		 * bitmap.getWidth(); int height = bitmap.getHeight(); int[] pixels =
		 * new int[width * height]; float[] HSV = new float[3]; // get pixel
		 * array from source bitmap.getPixels(pixels, 0, width, 0, 0, width,
		 * height);
		 * 
		 * int index = 0, R, G, B; // iteration through pixels for (int y = 0; y
		 * < height; y++) { for (int x = 0; x < width; x++) { if (x == xTouch &&
		 * y == yTouch) { index = y * width + x; R = Color.red(pixels[index]); G
		 * = Color.green(pixels[index]); B = Color.blue(pixels[index]);
		 * Toast.makeText( context, "" + pixels.length + " Image is touched @" +
		 * "x: " + xTouch + ", y: " + yTouch, Toast.LENGTH_SHORT).show();
		 * pixels[index] = Color.rgb(COLOR_MAX, COLOR_MAX, COLOR_MAX);
		 * 
		 * loop for next horizontal line for the pixel
		 * 
		 * for (int i = 0; i < 5; i++) { index++; index = y * width + x; R =
		 * Color.red(pixels[index]); G = Color.green(pixels[index]); B =
		 * Color.blue(pixels[index]); Toast.makeText( context, "" +
		 * pixels.length + " Image is touched @" + "x: " + xTouch + ", y: " +
		 * yTouch, Toast.LENGTH_SHORT).show(); pixels[index] =
		 * Color.rgb(COLOR_MAX, COLOR_MAX, COLOR_MAX);
		 * 
		 * } } } } Bitmap bitOut = Bitmap.createBitmap(width, height,
		 * Bitmap.Config.RGB_565); bitOut.setPixels(pixels, 0, width, 0, 0,
		 * width, height); imageView.setImageBitmap(bitOut);
		 */
		return false;
	}

}
=======
/**
 * 
 */
package com.neevtech.imageprocessing.listeners;

import android.content.Context;
import android.graphics.Bitmap;
import android.graphics.Color;
import android.graphics.drawable.BitmapDrawable;
import android.graphics.drawable.Drawable;
import android.view.MotionEvent;
import android.view.View;
import android.view.View.OnTouchListener;
import android.widget.ImageView;
import android.widget.Toast;

/**
 * @author Ashish R Agre
 * 
 */
public class SpotTouchListener implements OnTouchListener {

	private static int COLOR_MAX = 0XFF;
	private Context context;
	private ImageView imageView;

	public SpotTouchListener(ImageView imageView, Context context) {
		this.imageView = imageView;
		this.context = context;
	}

	@Override
	public boolean onTouch(View v, MotionEvent event) {
		/*
		 * int xTouch = (int) event.getX(); int yTouch = (int) event.getY();
		 * Drawable tempDrawable = imageView.getDrawable(); Bitmap bitmap =
		 * ((BitmapDrawable) tempDrawable).getBitmap(); int width =
		 * bitmap.getWidth(); int height = bitmap.getHeight(); int[] pixels =
		 * new int[width * height]; float[] HSV = new float[3]; // get pixel
		 * array from source bitmap.getPixels(pixels, 0, width, 0, 0, width,
		 * height);
		 * 
		 * int index = 0, R, G, B; // iteration through pixels for (int y = 0; y
		 * < height; y++) { for (int x = 0; x < width; x++) { if (x == xTouch &&
		 * y == yTouch) { index = y * width + x; R = Color.red(pixels[index]); G
		 * = Color.green(pixels[index]); B = Color.blue(pixels[index]);
		 * Toast.makeText( context, "" + pixels.length + " Image is touched @" +
		 * "x: " + xTouch + ", y: " + yTouch, Toast.LENGTH_SHORT).show();
		 * pixels[index] = Color.rgb(COLOR_MAX, COLOR_MAX, COLOR_MAX);
		 * 
		 * loop for next horizontal line for the pixel
		 * 
		 * for (int i = 0; i < 5; i++) { index++; index = y * width + x; R =
		 * Color.red(pixels[index]); G = Color.green(pixels[index]); B =
		 * Color.blue(pixels[index]); Toast.makeText( context, "" +
		 * pixels.length + " Image is touched @" + "x: " + xTouch + ", y: " +
		 * yTouch, Toast.LENGTH_SHORT).show(); pixels[index] =
		 * Color.rgb(COLOR_MAX, COLOR_MAX, COLOR_MAX);
		 * 
		 * } } } } Bitmap bitOut = Bitmap.createBitmap(width, height,
		 * Bitmap.Config.RGB_565); bitOut.setPixels(pixels, 0, width, 0, 0,
		 * width, height); imageView.setImageBitmap(bitOut);
		 */
		return false;
	}

}
>>>>>>> 89fda57b84f3f45b593875cb6cde08a56a510b8d
